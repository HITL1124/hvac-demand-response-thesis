# 验证报告

Date: 2026-05-04

## Checks Run

| Check | Result | Notes |
|---|---|---|
| Static old-path scan | PASS | `run/`, `export/`, `plot/`, `src/` 中未发现旧默认路径 `data/Stage1data`, `data/ReserveCostCurve_hourly`, `fullfile('data',...)` 等。 |
| Python syntax | PASS | `project_paths.py`, `run/stage1_cqr.py`, `run/stage1_cqr_plot.py` 通过 `py_compile`。 |
| Python path helper smoke test | PASS | `project_paths.STAGE1`, `project_paths.EXPORTS`, `stage1_cqr_for_stage2.mat` 均能解析到存在路径。 |
| Workbook inventory | PASS | 已生成 `93_导出Excel表头摘要.csv`，共扫描 63 个 sheet。 |
| MATLAB/Octave availability | NOT RUN | 当前环境未找到 `matlab` 或 `octave` 命令。 |
| Lightweight MATLAB export tests | NOT RUN | 因 MATLAB/Octave 不可用，未执行 `export_stage1_cqr_prediction_intervals_xlsx` 等轻量导出。 |

## 2026-05-05 追加检查

| Check | Result | Notes |
|---|---|---|
| Export root cleanup | PASS | `data/exports/` 根目录下已无 xlsx，只保留索引和 6 个按图号分组的目录。 |
| Export script output grouping | PASS | `export/*.m` 未发现默认输出到 `project_data_file('exports')` 根目录的 xlsx。 |
| Docs filename cleanup | PASS | `docs/` 下文件名已改为中文，文档内未发现旧英文清单文件名引用。 |

## 2026-05-06 追加检查

| Check | Result | Notes |
|---|---|---|
| First-entry document | PASS | 已新增 `docs/00_先看这里.md`，给出 5 分钟阅读顺序和哪些目录先不要看。 |
| Docs reading order | PASS | 人工阅读文档已按 `00_` 到 `05_` 编号；验证和机器清单放在 `90_` 以后。 |
| Data entry document | PASS | 已新增 `data/README.md`，区分主线数据和扩展数据。 |
| Generated cache cleanup | PASS | 已删除根目录和 `run/` 下的 `__pycache__`。 |
| Multiday Python syntax | PASS | `python -m py_compile run/stage1_cqr.py project_paths.py` 通过。 |
| Multiday day selection | PASS | `python run/stage1_cqr.py --multiday --select-only` 通过；当前测试集只有 `day_012` 和 `day_013` 两个严格 0:00 完整测试日。 |
| Multiday Stage1 export | HISTORICAL_PASS | 移动前曾生成 `data/stage1/test_days/day_012/stage1_cqr_for_stage2.mat` 和 `day_013/stage1_cqr_for_stage2.mat`；当前默认位置已改为旁路补充目录 `../99-paper-sim-cleanroom_supplement/data/stage1/test_days/`。 |
| Multiday MATLAB static check | HISTORICAL_PASS | 移动前 MATLAB Code Analyzer 检查 `run/run_multiday_robustness_beta90.m`、`project_paths.m`、`project_data_file.m` 无问题；当前多日脚本已移至 `../99-paper-sim-cleanroom_supplement/run/`。 |
| Multiday MATLAB smoke test | HISTORICAL_PASS | 移动前 `run_multiday_robustness_beta90(struct('maxDays',0))` 通过；当前运行前需先将 `P.supplement_root/run` 加入 MATLAB path。 |
| Multiday one-day run | TIMEOUT | `run_multiday_robustness_beta90(struct('maxDays',1))` 在 MCP 900 秒上限内只完成 day_012 baseline；reserve scan 未保存 MAT，随后终止该 MCP MATLAB 会话以避免继续占用 CPU。 |

## 2026-05-06 三层主线视图收敛检查

| Check | Result | Notes |
|---|---|---|
| Main cleanroom directory depth | PASS | 主线目录只保留到三层活跃视图：入口目录、`data/*`、以及必要的 `data/postprocess/*`、`data/stage1/gaussian`、`data/exports/Fig*`。 |
| Main archive removal | PASS | 主线目录下不再存在 `archive/`。历史材料已移至旁路目录 `../99-paper-sim-cleanroom_archive/`。 |
| Main supplement removal | PASS | 主线目录下不再存在 `data/stage1/test_days/`、`data/multiday_beta90_nscan20/`、`data/exports/07_multiday_robustness_beta90_nscan20/`。 |
| Multiday entry removal | PASS | `run/run_multiday_robustness_beta90.m` 和 `docs/05_多测试日稳健性说明.md` 已移至 `../99-paper-sim-cleanroom_supplement/`。 |
| Python multiday default path | PASS | `run/stage1_cqr.py --multiday` 默认写入 `project_paths.SUPPLEMENT_STAGE1_TEST_DAYS`，不再写入主线 `data/stage1/` 深层目录。 |
| Sibling directory pointers | PASS | `README.md`、`docs/00_先看这里.md`、`docs/01_仿真主线流程.md` 和 `data/README.md` 已说明主线、补充实验、历史归档的分工。 |

## 2026-05-05 完整 90 天 CQR 重跑

| Check | Result | Notes |
|---|---|---|
| 14 天旧结果归档 | PASS | 已将旧 `stage1_cqr_tree_data.mat`、`stage1_hour_model_for_stage2.mat`、`stage1_cqr_tree_tuned_parallel_results.mat`、`stage1_cqr_for_stage2.mat` 和旧 `test_days/` 移入旁路归档目录 `../99-paper-sim-cleanroom_archive/data/stage1/archive_14day_before_full_cqr_20260505/`。 |
| 输入数据长度 | PASS | `data/processed/processed_state.mat` 与 `data/processed/processed_input.mat` 均为 `8641` 点，约 `90.01` 天。 |
| Stage1 裁剪设置 | PASS | `run/Stage1Code_CQR.m` 中 `useFirstNDays = false`，本轮未裁剪前 14 天。 |
| MATLAB Stage1 重跑 | PASS | 已重新生成 `data/stage1/stage1_cqr_tree_data.mat`；验证 `T/U` 长度为 `8641`，`origin_idx = 8522`，`idx_origin_te = 1705`，`H = 96`。 |
| Stage2 模型兼容文件 | PASS | 已从完整数据 Stage1 模型矩阵补写 `data/stage1/stage1_hour_model_for_stage2.mat`，并已将该保存步骤固化进 `run/Stage1Code_CQR.m`，用于后续 Stage2 默认加载。 |
| Python CQR 重跑 | PASS | `python .\run\stage1_cqr.py --multiday` 完成 96 个 horizon 训练，并重新生成 `stage1_cqr_tree_tuned_parallel_results.mat` 与默认 `stage1_cqr_for_stage2.mat`。 |
| CQR 覆盖率摘要 | PASS | Test empirical coverage：0.80 -> 0.8869，0.85 -> 0.9281，0.90 -> 0.9594，0.95 -> 0.9838。 |
| 多测试日 CQR 输入 | PASS | 完整测试集严格 0:00 完整窗口候选日为 18 天；默认选择 5 天：`day_073`、`day_084`、`day_085`、`day_088`、`day_090`。 |
| Stage2 范围控制 | PASS | 本轮只运行到 CQR 结束，未运行 baseline、reserve scan、credible screen 或 postprocess。 |
| MATLAB Code Analyzer | PASS_WITH_EXISTING_WARNINGS | `run/Stage1Code_CQR.m` 可分析；报告的 unreachable/preallocation 信息为脚本原有结构问题，本轮新增 Stage2 模型保存段未引入阻断错误。 |

## Static Scope

静态路径扫描覆盖：

- `run/*.m`, `run/*.py`
- `export/*.m`
- `plot/*.m`
- `src/*/*.m`

旁路归档目录 `../99-paper-sim-cleanroom_archive/`、旁路补充目录 `../99-paper-sim-cleanroom_supplement/`、论文副本 `paper/` 和 Origin 副本 `origin/` 没有作为主线可运行代码扫描目标。
